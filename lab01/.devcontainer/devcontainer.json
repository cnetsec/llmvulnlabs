{
  "name": "hacktiba-2025-lab01",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "/bin/bash -lc \"python -m pip install --upgrade pip && python -m pip install -q --index-url https://download.pytorch.org/whl/cpu torch && python -m pip install -q transformers && python - <<'PY'\nimport torch, transformers\nprint('✅ Torch', torch.__version__, '| Transformers', transformers.__version__)\nPY\"",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
