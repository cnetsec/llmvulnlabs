{
  "name": "hacktiba-2025-labs",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "postCreateCommand": "python -m pip install --upgrade pip && for req in lab*/requirements.txt; do [ -f \"$req\" ] && pip install -r \"$req\"; done",

  "postStartCommand": "python - <<'PY'\nfrom importlib import import_module\nfor m in (\"torch\",\"transformers\"): \n  try:\n    mod = import_module(m)\n    print(f\"✅ {m}\", getattr(mod, \"__version__\", \"n/a\"))\n  except:\n    print(f\"ℹ️ {m} não instalado\")\nPY",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}
